spring:
  application:
    name: student-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/finalpt
    username: AleBrv
    password: 2282tobito?
  jpa:
    show-sql: true
    hibernate.ddl-auto: update